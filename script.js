document.getElementById('toggleCategories').addEventListener('click', function() {
    var subCategories = document.getElementById('subCategories');
    subCategories.classList.toggle('show'); // Toggle the 'show' class to display/hide subcategories
});
